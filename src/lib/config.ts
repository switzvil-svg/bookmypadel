// Central place for values that show up in both logic and copy (pricing
// page, checkout, slot-availability messaging) — change them here, never in
// the individual pages that display them.
export const BOOST_CONFIG = {
  PRICE_EUR: 29.99,
  DURATION_DAYS: 7,
  MAX_ACTIVE_SLOTS: 8,
} as const;
